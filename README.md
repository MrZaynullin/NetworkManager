# BS Network Manager

### Prerequisites

The following packages are needed to run Network Manager:
- python-pip
- nginx-extras (with ngx_lua module)

The following python libraries are needed (install via pip):
- pexpect
- pyrad